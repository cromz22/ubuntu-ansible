- hosts: localhost
  become: true
  gather_facts: false

  vars:
    snap_packages:
      - rclone

  tasks:
    - name: Install snap packages
      community.general.snap:
        name: "{{ item }}"
        state: present
      loop: "{{ snap_packages }}"
